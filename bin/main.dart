import 'package:projeto_dart/projeto_dart.dart'; // Importa a biblioteca projeto_dart

void main(List<String> arguments) {
  // A função main pode receber argumentos, estes argumentos são passados através do terminal
  print(
      calculate()); // Usando a função calculate, que está dentro da biblioteca projeto_dart

  // Da pra fazer o mesmo com os outros arquivos que estão na pasta src, mas eu to com preguiça :P
}
